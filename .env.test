# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'
# Doctrine appends "_test" in the test env (see config/packages/doctrine.yaml),
# so this resolves to the "watcha_test" database.
DATABASE_URL="mysql://app:app@127.0.0.1:3306/watcha?serverVersion=8.0&charset=utf8mb4"
